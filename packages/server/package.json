{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "[[ $NODE_ENV == \"production\" ]] && ts-node -r dotenv/config src/server.ts || nodemon --watch 'src/**/*.ts' --exec ts-node -r dotenv/config src/server.ts"
  },
  "dependencies": {
    "@types/cookie-parser": "^1.4.2",
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.2",
    "cookie-parser": "^1.4.4",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.2",
    "ts-node": "^8.6.0",
    "typescript": "^3.7.4"
  }
}
